# Google Analytics
VITE_GA_MEASUREMENT_ID=your-ga-measurement-id

# SMTP Configuration for Email Sending
# IMPORTANT: Use smtps.aruba.it (not smtp.aruba.it) for SSL connection
SMTP_HOST=smtps.aruba.it
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-email-password
SMTP_FROM_NAME=CodeCraft Studio

# Contact Email (where messages will be sent)
CONTACT_EMAIL=info@codecraft.it

# Environment
NODE_ENV=development
